machine:
  timezone: UTC
  java:
    version: oraclejdk8

general:
  artifacts:
    - target/universal

dependencies:
  cache_directories:
    - "target/resolution-cache"
    - "project/target/resolution-cache"
  override:
    - sbt test:compile

test:
  override:
    - sbt test
  post:
    - sbt dist
