machine:
  timezone: UTC
  java:
    version: oraclejdk8

dependencies:
  override:
    - sbt test:compile

test:
  cache_directories:
    - "target/resolution-cache"
    - "project/target/resolution-cache"
  override:
    - sbt test
  post:
    - sbt dist
